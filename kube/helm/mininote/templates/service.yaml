apiVersion: v1
kind: Service
metadata:
  name: {{ include "mininote.fullname" . }}
  labels:
    {{- include "mininote.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mininote.selectorLabels" . | nindent 4 }}
